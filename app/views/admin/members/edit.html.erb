<% @page_title = "ユーザ情報の編集" %>

<h3 style="border-style: groove; padding: 4px;"><%= @page_title %></h3>

<p><%= link_to "ユーザリストの詳細に戻る", [:admin, @member] %></p>

<%= form_for [:admin, @member] do |form| %>
<%= render "form", form: form %>
<p><%= form.submit %></p>
<% end %>
